[
  {
    "name": "/price with empty watchlist shows add prompt",
    "steps": [
      {
        "send": { "text": "/price" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "No coins on your watchlist yet — tap ➕ Add Coin to add one."
            }
          }
        ]
      }
    ]
  }
]
