[
  {
    "name": "/start replies with the welcome + main menu",
    "steps": [
      {
        "send": { "text": "/start" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "👋 Welcome! Tap a button below to get started." }
          }
        ]
      }
    ]
  },
  {
    "name": "Back to menu re-renders the main menu",
    "steps": [
      {
        "send": { "callback": "menu:main" },
        "expect": [{ "method": "editMessageText" }]
      }
    ]
  }
]
